# vertical-farming-door-prediction
Machine learning project to predict door open/closed status in a vertical farming cube using temperature and humidity sensor data. Includes data preprocessing, EDA, feature scaling, and model comparison using Random Forest, SVM, and XGBoost.
